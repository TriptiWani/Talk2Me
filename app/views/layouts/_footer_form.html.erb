<% if @current_user.present? %>
  <div id="footer">
    <div class="col-xs-12 navbar-inverse navbar-fixed-bottom">
      <div class="row" id="bottomNav">
        <div class="col-xs-2 col-xs-offset-2 text-center footer_options">
          <%= link_to  'Contacts' , users_path ,:class => 'glyphicon glyphicon-user'%>
        </div>
        <div class="col-xs-2 col-xs-offset-1 text-center footer_options">
          <%= link_to  'Groups' , groups_path ,:class => 'glyphicon glyphicon-heart'%>
        </div>
        <div class="col-xs-2 col-xs-offset-1 text-center footer_options">
          <%= link_to  'Settings' , edit_user_path(@current_user) ,:class => 'glyphicon glyphicon-cog'%>
          <!-- <a href="#">Settings<i class="glyphicon glyphicon-cog"></i>
        </a>-->
      </div>
      </div>
    </div>
  </div>
<% end %>
